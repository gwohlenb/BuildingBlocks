#!/usr/bin/env node

var app = require ('./../app');
var port = process.env.PORT || 3000; // Establish a dynamically allocated port (if Heroku) or a static port otherwise

// Bind to the designated port
app.listen(port, function() {
	console.log('Listening on port ' + port);
});
